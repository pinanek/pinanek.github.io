<thead><slot /></thead>

<style>
  thead {
    --border-styles:  1px solid var(--color-base-default);
  }

  thead :global(tr th) {
    background-color: var(--color-base-tertiary);
    padding: var(--spacing-0_5) var(--spacing-2);
  }

  thead :global(tr th:not(:first-child):not(:last-child)) {
   border-right: var(--border-styles);
   border-left: var(--border-styles);
  }

  thead :global(tr th:last-child) {
    border-top-right-radius: var(--radii-md);
  }

  thead :global(tr th:first-child) {
    border-top-left-radius: var(--radii-md);
  }
</style>
