---
title: Example post
description: Just an example post 😂. This post shows all kind of styled markdown components
seoTitle: Example post
categories: [Example]
image:
  src: images/_thumbnail.png
  alt: A lonely car
  caption: A lonely car
dates:
  published: 09-14-2022
  updated: 12-12-2022
---

## Headings

## Heading 2

### Heading 3

#### Heading 4

## Horizontal Rule

---

## Paragraph

"Your time is limited, so don't waste it living someone else's life. Don't be trapped by dogma – which is living with
the results of other people's thinking." - Steve Jobs

## Emphasis

This is **bold text**

This is _italic text_

This is **_italic text_**

This is ~~strike-through text~~

## Link

This is a [in-site link](/), and this is an [external link](https://www.youtube.com/watch?v=dQw4w9WgXcQ).

## Image

![Hello](images/_default-logo.png "Hello")

![Hello](images/_hello.png "Hello")

## Blockquote

> This is a blockquote.
>
> Hello there 💖.

Another one:

> This is a blockquote
>
> > With a nested blockquote

## Inline code

This is an `inline code`.

This is an `let inline_code= "inline code"`{lang=rs} with syntax highlighting.

## Codeblock

```rs title=example.rs
use utils::read_lines;
use regex::Regex;

const HELLO: &str = "Hello there 💖";

// This is a comment

struct Template<'a> {
  source: &'a str;
}

impl<'a> Template {
  fn new(source: &'a str) -> Template {
    Template{ source }
  }
}

fn main() {
  let num: i32 = 3;
  let re = Regex::new(r"^\d{4}-\d{2}-\d{2}$").unwrap();
  let lines: Vec<String> = read_lines("input.txtakskdjalskdjaskldjasldjaslkdjaslkdjaslkdjaslkdjalskdjalskdjsalkasdjalskjdlaskjdlkasjdk");

  let template = Template::new("🎉 Hurray!");

  if num == 3 {
    println!("{num}");
  }
}
```

```html
<div>
  <p>Hello</p>
</div>
```

```tsx
import * as React from "react";

interface Props {
  children: React.ReactNode;
}

const string = "This is a string";
const number = 302323;
const regex = /\b([-\w]+)(?:=(?:"([^"]*)"|'([^']*)'|([^"'\s]+)))?/g;

// This is the CodeBlock component
function CodeBlock({ children }: Props): JSX.Element {
  if (string === "This is a string") {
    console.log(string);
  } else {
    console.log("Error");
  }

  return (
    <div>
      <h2>This is an code block with syntax highlighting 😍</h2>
      {children}
    </div>
  );
}
```

```css title=highlighter.css
article {
  --kenanip-theme-background: var(--color-base-lighter);
  --kenanip-theme-text: var(--color-text-default);
  --kenanip-theme-token-constant: var(--kenanip-theme-text);
  --kenanip-theme-token-variable-parameter: var(--kenanip-theme-text);
  --kenanip-theme-token-punctuation: var(--kenanip-theme-text);
  --kenanip-theme-token-html-xml-tag: var(--kenanip-theme-text);
}

[color-scheme="light"] article {
  --kenanip-theme-token-keyword: hsl(210, 96%, 40%);
  --kenanip-theme-token-type: hsl(350, 60%, 55%);
  --kenanip-theme-token-string-expression: hsl(153, 39%, 41%);
  --kenanip-theme-token-number: hsl(210, 65%, 60%);
  --kenanip-theme-token-comment: hsl(0, 0%, 50%);
  --kenanip-theme-token-function: hsl(var(--base-hue), 60%, 40%);
  --kenanip-theme-token-control-flow: hsl(261, 55%, 50%);
  --kenanip-theme-token-css-attribute-quantifier-regex: hsl(1, 81%, 73%);
  --kenanip-theme-token-string-regex: hsl(21, 69%, 73%);
}

[color-scheme="dark"] article {
  --kenanip-theme-token-keyword: hsl(210, 60%, 70%);
  --kenanip-theme-token-type: hsl(350, 70%, 80%);
  --kenanip-theme-token-string-expression: hsl(153, 39%, 65%);
  --kenanip-theme-token-number: hsl(210, 65%, 80%);
  --kenanip-theme-token-comment: hsl(0, 0%, 50%);
  --kenanip-theme-token-function: hsl(var(--base-hue), 69%, 70%);
  --kenanip-theme-token-control-flow: hsl(261, 55%, 75%);
  --kenanip-theme-token-css-attribute-quantifier-regex: hsl(1, 81%, 73%);
  --kenanip-theme-token-string-regex: hsl(21, 69%, 83%);
}
```

## List

This is an unordered list:

- One.
- Two.
- Three.
- Four.
- And so on 😊.

This is an ordered list:

1. One.
2. Two.
3. Three.
4. Four.
5. And so on 😊.

## Table

|                        First column                         | Second column | Third column |
| :---------------------------------------------------------: | :-----------: | :----------: |
|                             One                             |      Two      |    Three     |
|                              1                              |       2       |      3       |
| Long long long long long long long long long long long data |               |              |

The background color should be `#ffffff` for light mode and `#0d1117` for dark mode.

## Long long long long long long long heading

Just a test.

## Another heading

Another heading

## Another heading 1

Another heading

## Another heading 2

Another heading

## Another heading 3

Another heading

## Another heading 4

Another heading
