---
import * as styles from './inline-code.css'

type Props = astroHTML.JSX.HTMLAttributes

const { class: className, ...props } = Astro.props as Props
---

<code class:list={[styles.main, className]} {...props}><slot /></code>
