import { breakpoint } from './breakpoint'
import { color } from './color'
import { spacing } from './spacing'
import { duration, ease } from './transition'
import { font, fontSize, fontWeight, lineHeight } from './typography'
import { radii } from './radii'

const tokens = {
  breakpoint,
  color,
  spacing,
  duration,
  ease,
  font,
  fontSize,
  fontWeight,
  lineHeight,
  radii
}

export default tokens
